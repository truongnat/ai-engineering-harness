# v0.10.7 — Codex plugin packaging

Codex is **not** a project-local slash-command provider. Native surface is `.codex-plugin/plugin.json` + `skills/`, installed via Codex `/plugins` when published.

Project `npx install --provider codex` remains **AGENTS.md + `.ai-harness/` fallback only**.

See [codex-plugin-support.md](codex-plugin-support.md).
