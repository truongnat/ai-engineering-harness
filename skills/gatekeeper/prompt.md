Review VERIFY, REVIEW, and ship blocker evidence.

Decide:

- allow ship
- block ship
- defer for human decision

Do not modify implementation files.
Return explicit next command and reason.

Run `node hooks/core/guard-phase.js --command harness-ship --session <active-session> --json` when available.
