Run fresh verification checks and record evidence in VERIFY.md and tool-run artifacts.

Use `hooks/core/record-tool-output.js` after each verification command when hooks are available.

Stop with blocked output when evidence is missing.
